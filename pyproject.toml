[project]
name = "bank-analyzer"
version = "0.0.1"
description = "First full project for Git"
requires-python = ">=3.13"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[tool.setuptools.packages.find]
where = ["src"]